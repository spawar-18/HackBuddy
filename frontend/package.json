{
  "name": "frontend-proxy",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd hackbuddy && npm install && npm run build && cd .. && mkdir -p dist && cp -r hackbuddy/dist/. dist/"
  }
}
